# Typescript Week 01 개인 정리

## ✏️ 배운 내용 요약

# Section 01

## Typescript

- 자바스크립트를 더 안전하게 사용할 수 있도록 타입 관련 기능들을 추가한 언어
- 대규모 application을 만들 때 자바스크립트를 더욱 안정적으로 사용하기 위해 타입이라는 안전장치를 두게 됨
- 점진적 타입 시스템

# Section 02

## 기본 타입(내장 타입)

## 원시 타입(Primitive Type)

- 하나의 값만 저장하는 타입

### number

- 숫자를 의미하는 모든 값을 포함하는 타입

### string

- 문자열 값을 포함하는 타입

### boolean

- 참과 거짓만 저장하는 타입

### null

### undefined

- null과 undefined도 타입으로 지정 가능
- tsconfig.json의 strictNullChecks 값을 통해 다른 타입에 null값 부여 가능

## 리터럴 타입

- 값 그 자체가 타입이 되는 경우

```ts
let numA: 10 = 10;
let srtA: "hello" = "hello";
let boolA: true = true;
```

## 배열 타입

```ts
let numArr: number[] = [1, 2, 3];
let strArr: string[] = ["hello", "bye"];
let boolArr: Array<boolean> = [true, false];
```

## 배열에 들어가는 요소들의 타입이 다양한 경우

```ts
let multiArr: (string | number)[] = [1, "hello"];
```

## 다차원 배열의 타입을 정의하는 방법

```ts
let doubleArr: number[][] = [
  [1, 2, 3],
  [4, 5],
];
```

## 튜플 타입(길이와 타입이 고정된 배열)

- 순서랑 길이가 타입 설정과 입력된 값이 동일해야 함
- 입력해야하는 값의 순서와 길이가 지정되어 있을 경우

```ts
let tup1: [number, number] = [1, 2];
let tup2: [number, string, boolean] = [3, "hello", true];
```

## 객체 타입

- object로도 타입 지정이 가능하나 내부 요소에 접근 불가능

### 객체 리터럴 타입

- 구조적 타입 시스템 -> Property Based Type System

```ts
let user: {
  id: number;
  name: string;
  age?: number; // Optional Property
} = {
  id: 1,
  name: "name",
  age: 15,
};
```

```ts
let config: {
  readonly apiKey: string; // 수정 불가능을 readonly로 표시(읽기 전용 property)
} = {
  apiKey: "MY API KEY",
};
```

## 타입 별칭(type alias)

- 같은 구조를 갖는 객체에 타입 구조를 일괄적으로 지정해주기 위해 사용
- 같은 스코프 내에서 중복된 이름의 타입 별칭은 선언 불가능

## 인덱스 시그니처

- 키와 밸류의 규칙을 기준으로 객체의 type을 정의할 수 있는 문법
- 규칙을 위반하지만 않으면 모든 객체를 허용하는 타입(비어있어도 허용되므로 주의)
-

```ts
type CountryCodes = {
  [key: string]: string;
};

let countryCodes: CountryCodes = {
  Korea: "ko",
  UnitedState: "us",
  UnitedKingdom: "uk",
};
```

```ts
type CountryNumberCodes = {
  [key: string]: number;
  Korea: number; // 인덱스 시그니처로 인해 [key:string]:number만 지켜지면 모든 객체를 허용하지만 Korea:number 타입은 반드시 객체 안에 존재해야 함
};

type CountryNumberCodes = {
  [key: string]: number;
  Korea: string; // 반드시 포함해야 할 값으로 지정한 속성의 value값은 인덱스 시그니처로 지정한 value값과 일치하거나 호환해야함(에러 발생)
};
```

## 열거형 타입(Enumerable Type)

- 여러가지 값들에 각각 이름을 부여해 열거해두고 사용하는 타입
- enum은 typescript에만 존재하지만 컴파일 시 삭제되지 않음(직접적인 값으로 사용되기 때문 -> 객체로 모습만 변환돼서 사용됨)

```ts
enum Role {
  // 숫자형 enum
  ADMIN, // 숫자로 지정해주지 않아도 0번부터 자동으로 매칭됨
  USER, // ex) ADMIN을 10번으로 지정하면 USER은 11 GUEST는 12
  GUEST, // ex) USER을 10번으로 지정하면 ADMIN은 0 GUEST는 11
}

const user1 = {
  name: "이정용",
  role: Role.ADMIN, // <- 관리자
};

const user2 = {
  name: "홍길동",
  role: Role.USER, // <- 일반 유저
};

const user3 = {
  name: "김철수",
  role: Role.GUEST, // <- 게스트
};
```

## any

- 특정 변수의 타입을 확실히 모를 때
- 모든 타입의 값, 모든 타입의 변수에 넣을 수 있는 치트키같은 값
- 사실상 타입 검사를 안하는것과 같음 -> 런타임에 에러 발생 가능성 높음
- 최대한 사용 안하는 편이 좋음

## unknown

- any 타입과 비슷하지만 타입의 값으로 집어넣는건 불가능
- 특정 타입의 메서드도 사용하지 못함
- typeof(타입 정제, 타입 좁히기)를 통해 type을 지정해줘야 해당 타입으로 사용 가능

## void

- 함수의 반환값의 타입을 지정할 때 return값이 없을 때 타입 지정
- 변수에 지정된 void 타입에는 undefined만 집어 넣을 수 있음

## never

- 존재하지 않는, 불가능한 타입
- 해당 함수의 반환값이 있는 것이 불가능 할 경우(무한루프, throw new Error() 등)
- 변수의 타입으로 never을 정의하면 어떠한 값도 담을 수 없음

# Section 03

- 모든 타입들은 부모와 자식 관계로 이루어져 있음(타입 계층도)
- 부모타입은 슈퍼타입, 자식타입은 서브타입이라고 부름
- 부모는 자식타입들을 다 사용할 수 있음(up casting)
- 자식이 부모타입을 사용하는건 일부 상황을 빼고는 불가능함(down casting)
- unknown = 전체 집합(모든 타입의 부모) -> 어느 값이든 사용 가능
- never = 공집합(모든 타입의 자식) -> 어느 값도 사용 불가능, 함수의 반환값 type(무한루프, 바로 에러를 throw 등)만 사용 가능
- any 타입은 다운 캐스팅이 가능함(never은 불가능)
- 타입 계층을 다 무시하는 타입이기 때문에 왠만하면 안쓰는게 좋음
- 다운캐스팅은 추후 가능 case들을 다룰 예정(아마도?)

## 기본 타입간의 호환성

- 특정 타입을 다른 타입으로 취급해도 괜찮은지 판단하는 것

```ts
let num1: number = 10;
let num2: 10 = 10;

num1 = num2; // num1이 슈퍼타입 업 캐스팅
```

## 객체 타입간의 호환성

- 어떤 객체타입을 다른 객체타입으로 취급해도 괜찮은지 판단하는 것

```ts
type Animal = {
  name: string;
  color: string;
};

type Dog = {
  name: string;
  color: string;
  breed: string;
};

let animal: Animal = {
  name: "기린",
  color: "yellow",
};

let dog: Dog = {
  name: "돌돌이",
  color: "brown",
  breed: "진도",
};

animal = dog; // 가능(animal 슈퍼 dog 서브 up-casting)
dog = animal; // 불가능(dog 서브 animal 슈퍼 down-casting)
```

## 대수 타입

- 여러개의 타입을 합성해서 새롭게 만들어낸 타입
- 합집합 타입과 교집합 타입이 존재

1. 합집합 - Union 타입

```ts
let a: string | number; // string 값도 넣을 수 있고 number 값도 넣을 수 있음
```

2. 교집합 - Intersection 타입

- 일반적으로 기본 타입들은 겹치는 경우가 대부분 없이 때문에 never 타입이 될 가능성이 큼
- 객체타입에서 주로 사용

```ts
let variable: number & string; // never

type Dog = {
  name: string;
  color: string;
};

type Person = {
  name: string;
  language: string;
};

type Intersection = Dog & Person;

// type Intersection은 name:string; color:string; language:string;
```

## 타입 추론

- 변수에 타입이 지정되어 있지 않아도 초깃값을 기준으로 타입을 추론
- 프로퍼티를 기준으로 호환성을 판단
- Animal에 비해 Dog 타입은 breed라는 추가 property를 가지고 있기 때문에 Animal이 슈퍼타입, Dog가 서브타입이 됨

## 타입 단언

- 값 as 단언 <- 단언식

### 타입 단언의 규칙

- A as B
- A가 B의 슈퍼타입거나
- A가 B의 서브타입이어야 함

### 다중 단언

- 가능하나 좋은 방법은 아님

```ts
let num1 = 10 as unknown as string;
```

### const 단언

- 변수 선언에 const 단언을 하면 리터럴 타입이 됨
- 객체에 const 단언을 하면 readonly property가 되어서 외부에서 수정할 수 없게 할 수 있음

```ts
let num2 = 10 as const;
```

### Non Null 단언

- 어떤 값이 null이거나 undefined가 아니라고 타입스크립트 컴파일러에게 알려주는 역할을 함

## 타입 좁히기

- 조건문 등을 이용해 넓은 타입에서 좁은 타입으로 타입을 상황에 따라 좁히는 방법

### 타입 가드

- if (typeof A === 'type')
- A instanceof C = A가 C 객체인지 확인(C는 메서드)
- A && 'property' in A = type 객체 조건 부여

## 서로소 유니온 타입(taged union type)

- 교집합이 없는 타입들로만 만든 유니온 타입
- 구분 property(리터럴타입)를 사용해서 서로소 관계를 만들어서 구분해서 사용

# Section 04

## 함수 타입

### 화살표 함수의 타입 정의

```ts
const add = (a: number, b: number) => a + b; // 타입 추론
```

## 함수 타입 표현식과 호출 시그니쳐

### 함수 타입 표현식

```ts
type Props = (a: number, b: number) => number;

const add: Props = (a, b) => a + b;
const sub: Props = (a, b) => a - b;
const mul: Props = (a, b) => a * b;
const div: (a: number, b: number) => number = (a, b) => a / b;
```

### 호출 시그니쳐(call signature)

```ts
type Props2 = {
  (a: number, b: number): number;
};

const add2: Props2 = (a, b) => a + b;
const sub2: Props2 = (a, b) => a - b;
const mul2: Props2 = (a, b) => a * b;
const div2: (a: number, b: number) => number = (a, b) => a / b;
```

## 함수 타입의 호환성

- 특정 함수 타입을 다른 함수 타입으로 취급해도 괜찮은가를 판단

1. 반환값의 타입이 호환되는가 (up-casting 가능 down-casting 불가능)
2. 매개변수의 타입이 호환되는가 (up-casting 불가능 down-casting 가능, 매개변수의 개수가 다를때는 적은쪽에서 큰쪽으로만 가능)

## 함수 오버로딩

- 함수를 매개변수의 개수나 타입에 따라 여러가지 버전으로 정의하는 방법

```ts
// 버전들 -> 오버로드 시그니쳐
function func(a: number): void;
function func(a: number, b: number, c: number): void;

// 실제 구현부 -> 구현 시그니쳐
function func(a: number, b?: number, c?: number) {
  if (typeof b === "number" && typeof c === "number") {
    console.log(a + b + c);
  } else {
    console.log(a * 20);
  }
}
```

## 사용자 정의 타입가드

```ts
type Dog = {
  name: string;
  isBark: boolean;
};

type Cat = {
  name: string;
  isScratch: boolean;
};

type Animal = Dog | Cat;

function isDog(animal: Animal): animal is Dog {
  return (animal as Dog).isBark !== undefined;
}

function isCat(animal: Animal): animal is Cat {
  return (animal as Cat).isScratch !== undefined;
}

function warning(animal: Animal) {
  if (isDog(animal)) {
    animal; // Dog
  } else if (isCat(animal)) {
    animal; // Cat
  }
}
```

# Section 05

## 인터페이스

- 타입에 이름을 지어주는 또 다른 문법 + 객체의 구조를 정의하는데 특화된 문법 (상속, 합침 등의 특수강 기능 제공)
- union, intersection 타입을 직접 만들수는 없음(타입 별칭에 활용, 타입 주석에 활용은 가능)

## 인터페이스 확장하기(상속)

- 상속하려는 interface의 property가 같은 property를 갖는 경우 최소 원본 property의 sub-type을 가져야 상속 가능

```ts
interface Animal {
  name: string;
  age: number;
}

interface Dog extends Animal {
  isBark: boolean;
}

interface Cat extends Animal {
  isScratch: boolean;
}

interface Chicken extends Animal {
  isFly: boolean;
}
```

## 인터페이스 선언 합치기

- 같은 이름의 interface를 여러 개 선언하면 결국 합쳐짐
- 충돌은 허용하지 않음(같은 property 다른 type)

```ts
interface Person {
  name: string;
}

interface Person {
  age: number;
}

const person: Person = {
  name: "he",
  age: 16,
};
```

## 💡 느낀 점

타입 스크립트에 대해 잘 모를때는 별로 안어렵다고 생각했는데 막상 배우고 나니 생각보다 복잡함

## 💡 어려운 점

여러 타입스크립트 문법들을 깊이 들어가니 생각보다 복잡해서 어려움
